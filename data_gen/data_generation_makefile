for h in training_data/training_data/*; do
  output=$(basename "$h" .sp)  # Removes path and .sp extension
  echo "echo \"Processing: $output\""
  echo "python3 data_generation_fixed.py -spice_netlist $h -voltage_file phase1_output_voltages/${output}.v  -output features"
done
